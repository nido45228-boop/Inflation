# 專案實作與規格總結報告

本文件旨在彙整「AirTabula 機票追蹤器」專案從概念發想、規格定義到視覺重構的完整歷程。所有內容均透過 OpenSpec 方法論進行管理。

---

## 1. 變更提案 (Proposal)

### 變更名稱
`apply-visual-redesign`

### 1.1. 意圖 (Intent)

目前的「機票價格追蹤器」`index.html` 頁面雖然功能完整（已成功串接後端 SQLite 資料庫並透過 Fetch API 進行非同步數據交換），但其視覺呈現仍處於未經設計的原始狀態。本提案旨在將 `DESIGN.md` 中定義的品牌設計語言，全面應用於此頁面，使其從一個功能原型（prototype）轉變為一個具備一致性、專業感與品牌識別度的產品介面。

### 1.2. 範疇 (Scope)

#### 範圍內 (In Scope)
*   對 `index.html` 的 9 個核心視覺元素進行全面的 CSS 與 HTML 結構調整。
*   嚴格遵守 `DESIGN.md` 中定義的顏色、字體、間距、按鈕層級與元件樣式。
*   CSS 命名將遵循 BEM (Block Element Modifier) 原則或 `DESIGN.md` 中指定的規範。

#### 範圍外 (Out of Scope)
*   嚴禁修改任何現有的 JavaScript 邏輯，特別是 `fetch`/`async`/`await` 相關的數據處理部分。
*   不新增任何額外的功能（例如：使用者認證、數據導出等）。
*   不修改後端 API (`/api/tickets`, `/api/insert`) 的行為或回傳格式。

### 1.3. 技術實作方法 (Approach)

1.  **CSS 變數化**: 在 `style.css` 的頂部建立 `:root` 選擇器，將 `DESIGN.md` 中定義的品牌色、字體和間距單位定義為 CSS 自訂屬性。
2.  **HTML 結構調整**: 為 `index.html` 中的目標元素添加符合 BEM 規範的 `class` 名稱。
3.  **樣式覆寫**: 撰寫新的 CSS 規則，針對新添加的 `class` 進行樣式設定。
4.  **主標題統一**: 將頁面主標題 `<h1>` 的內容統一修改為「雲端機票管家」。
5.  **迭代實作**: 依照 `tasks.md` 中規劃的三階段順序進行開發。

---

## 2. 設計規格書 (Design Specification)

### 2.1. 核心哲學

*   **數據中立 (Data Neutrality)**: 介面不應帶有引導性或情緒性的設計。我們只呈現原始數據，將詮釋權完全交還給使用者。
*   **形式追隨功能 (Form Follows Function)**: 視覺設計必須服務於數據呈現的清晰度與準確性。
*   **不打擾原則 (Non-Intrusive)**: 避免任何非使用者主動觸發的彈窗、廣告或行銷提示。

### 2.2. 設計規範

| 類別 | 規範說明 |
| :--- | :--- |
| **佈局 (Layout)** | 內容包裹於 `960px` 寬的置中容器內，具備適度留白。背景使用低飽和度品牌色 (`#F8F9FA`)。 |
| **標題 (Typography)** | 主標題 `<h1>` 使用品牌主字體，字重 `700`。數據欄位（價格、年份）使用等寬字體。 |
| **表單 (Forms)** | 輸入框 `input` 在 `:focus` 狀態下，邊框變為品牌主色，提供明確視覺反饋。 |
| **按鈕 (Buttons)** | 主要按鈕 `.btn--primary` 使用品牌主色背景。次要按鈕則採用較不突出的樣式。 |
| **列表 (Tables)** | 表格採用斑馬紋樣式，提高橫向可讀性。 |
| **標籤 (Badges)** | 價格欄位使用柔和的圓角背景標籤，使其在視覺上成為獨立數據單元。 |
| **反饋 (Feedback)** | 空狀態 `.empty-state` 僅顯示中性提示文字，不使用插圖或情緒化文案。 |

---

## 3. 實作任務清單 (Task Breakdown)

此清單記錄了從 `proposal.md` 與 `design.md` 拆解出的具體實作項目。

### Phase 1: 視覺骨幹 (Foundation)

- [x] **Task #1: 頁面容器與背景**
  - **意圖:** 建立具備品牌色調與適度留白的數據容器。
- [x] **Task #2: H1 主標題 (雲端機票管家)**
  - **意圖:** 將標題改為「雲端機票管家」，並套用專屬樣式。
- [x] **Task #9: 數據空狀態 (Empty State)**
  - **意圖:** 當無資料時，呈現符合品牌調性的視覺提示。

### Phase 2: 互動元件 (Interactive Components)

- [x] **Task #3: 機票輸入框 (Input)**
  - **意圖:** 重新設計輸入框外觀，強調輸入時的聚焦感。
- [x] **Task #4: 紀錄新增按鈕 (Submit)**
  - **意圖:** 移除預設樣式，套用品牌主色與 Hover 效果。
- [x] **Task #7: 資料操作按鈕 (Delete/Action)**
  - **意圖:** (預留任務) 重新設計表格內的動作按鈕。

### Phase 3: 數據列表與統計 (Data & Stats)

- [x] **Task #5: 機票數據表格 (Table/List)**
  - **意圖:** 將 `<tr>` 改造成具備間距與條紋背景的優雅列表。
- [x] **Task #8: 數據總結欄 (Summary/Stats)**
  - **意圖:** (預留任務) 為數據統計套用品牌指定的字體層級。
- [x] **Task #6: 價格標記 (Price Badge/Indicator)**
  - **意圖:** 針對價格欄位，使用品牌定義的標籤樣式。

---

## 4. 總結

透過 OpenSpec 的結構化流程，我們成功地將一個功能原型，依照明確的設計規格，系統性地轉化為一個具備品牌識別度的產品介面。整個過程從高層次的**提案**，到具體的**設計規範**，再到可執行的**任務清單**，最後完成**程式碼實作**，確保了最終產出與初始意圖的高度一致性。
